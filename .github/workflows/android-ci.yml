name: Android CI

on:
  push:
    branches: [main]
    paths:
      - 'app/**'
      - 'gradle/**'
      - '*.gradle.kts'
      - 'gradle.properties'
  pull_request:
    paths:
      - 'app/**'
      - 'gradle/**'
      - '*.gradle.kts'
      - 'gradle.properties'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - uses: gradle/actions/setup-gradle@v4

      - name: Build libxposed api to mavenLocal
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 10
          max_attempts: 3
          command: ./libxposed/api/gradlew :api:publishApiPublicationToMavenLocal -x :checks:compileKotlin --no-daemon

      - name: Build libxposed service to mavenLocal
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 10
          max_attempts: 3
          command: ./libxposed/service/gradlew :interface:publishInterfacePublicationToMavenLocal :service:publishServicePublicationToMavenLocal --no-daemon

      - name: Lint check
        run: ./gradlew ktlintCheck

      - name: Build APKs
        run: ./gradlew assembleDebug assembleRelease

      - name: Upload debug APK
        if: github.event_name != 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: phdp-debug
          path: app/build/outputs/apk/debug/*.apk
          retention-days: 7
